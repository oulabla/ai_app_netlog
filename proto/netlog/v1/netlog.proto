// Code generated by add_service.sh / make add-service. DO NOT EDIT MANUALLY.

syntax = "proto3";

package netlog.v1;

option go_package = "github.com/oulabla/go-base/gen/go/netlog/v1;netlogpb";

import "google/api/annotations.proto";
import "protoc-gen-openapiv2/options/annotations.proto";
import "google/protobuf/timestamp.proto";
import "google/protobuf/struct.proto";

// netlogService — основной сервис netlog (v1)
service netlogService {


   // CreateNetlog — создание записи
  rpc CreateNetlog (CreateNetlogRequest) returns (CreateNetlogResponse) {
    option (google.api.http) = {
      post: "/v1/netlog/create",
      body: "netlog"
    };
    option (grpc.gateway.protoc_gen_openapiv2.options.openapiv2_operation) = {
      summary     : "Создание записи в сервисе (v1)"
      description : "Возвращает id"
      operation_id: "netlog_v1_create"
      responses   : {
        key   : "200"
        value : {
          description : "Успешный ответ"
        }
      }
    };
  }

  // ListNetlog — получение списка записей с пагинацией и фильтрами
  rpc ListNetlog (ListNetlogRequest) returns (ListNetlogResponse) {
    option (google.api.http) = {
      post: "/v1/netlog/list",
      body: "*"
    };
    option (grpc.gateway.protoc_gen_openapiv2.options.openapiv2_operation) = {
      summary     : "Получение списка записей netlog с пагинацией"
      description : "Пагинация по last_id (cursor-based), сортировка DESC по id"
      operation_id: "netlog_v1_list"
      responses   : {
        key   : "200"
        value : {
          description : "Список записей и курсор для следующей страницы"
        }
      }
    };
  }

    // GetNetlog — получение записи по ID
  rpc GetNetlog (GetNetlogRequest) returns (GetNetlogResponse) {
    option (google.api.http) = {
      get: "/v1/netlog/{id}"
    };
    option (grpc.gateway.protoc_gen_openapiv2.options.openapiv2_operation) = {
      summary     : "Получение записи netlog по ID"
      description : "Возвращает одну запись по первичному ключу"
      operation_id: "netlog_v1_get"
      responses   : {
        key   : "200"
        value : {
          description : "Успешный ответ"
        }
      }
      responses   : {
        key   : "404"
        value : {
          description : "Запись не найдена"
        }
      }
    };
  }

}


message CreateNetlogRequest {
  Netlog netlog = 1;
}

message CreateNetlogResponse {
  int64 id = 1;
}


message Netlog {
    // ID заполняется только при ответе, при создании не указывается
    int64 id = 1;
    
    // Время создания, если не указано - будет NOW()
    google.protobuf.Timestamp created_at = 2;
    
    // Массив ключевых слов
    repeated string keywords = 3;
    
    // Параметры в формате JSON (структура может быть любой)
    google.protobuf.Struct parameters = 4;
    
    // Ошибка (если есть)
    optional string error = 5;
    
    // Количество элементов до AI фильтрации
    int32 num_before_ai_filter = 6;
    
    // Количество элементов после AI фильтрации
    int32 num_after_ai_filter = 7;
    
    // Результат до AI фильтрации
    google.protobuf.Struct result_before_ai_filter = 8;
    
    // Итоговый результат
    google.protobuf.Struct result = 9;
    
    // ID клиента (UUID)
    string client_id = 10;
    
    // Название приложения
    string app_name = 11;  
}

message ListNetlogRequest {
  // Пагинация
  optional int64 last_id = 1;           // если > 0 → возвращать записи где id < last_id
  int32 limit = 2;                      // обязательно, рекомендуемый диапазон 20..200

  // Фильтры (все optional)
  optional string client_id     = 10;   // uuid как строка
  optional string app_name      = 11;

  repeated string keywords      = 12;   // содержит ВСЕ указанные слова (array @>)

  optional bool has_error       = 13;   // true → error != null, false → error == null

  optional int32 min_before_ai  = 14;
  optional int32 max_before_ai  = 15;

  optional google.protobuf.Timestamp from_time = 16;
  optional google.protobuf.Timestamp to_time   = 17;
}

message ListNetlogResponse {
  repeated Netlog items = 1;

  // Курсор для следующей страницы
  optional int64 next_last_id = 2;      // id самой старой записи в items (или пусто, если больше нет)

  // Полезно для UI
  bool has_more = 3;                    // true, если вероятно есть ещё записи
}

message GetNetlogRequest {
  int64 id = 1; // ID записи
}

message GetNetlogResponse {
  Netlog netlog = 1;
}